<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gk.university.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="com.gk.university.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Jul 22 19:04:34 CST 2019.
        -->
        <id column="ID" jdbcType="BIGINT" property="id"/>
        <result column="TITLE" jdbcType="VARCHAR" property="title"/>
        <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate"/>
        <result column="GMT_MODIFIED" jdbcType="BIGINT" property="gmtModified"/>
        <result column="CREATOR" jdbcType="INTEGER" property="creator"/>
        <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount"/>
        <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount"/>
        <result column="LIKE_COUNT" jdbcType="INTEGER" property="likeCount"/>
        <result column="TAG" jdbcType="VARCHAR" property="tag"/>
    </resultMap>
    <update id="incView" parameterType="com.gk.university.model.Question">
    update question
    set
    VIEW_COUNT = VIEW_COUNT + #{viewCount,jdbcType=INTEGER}
    where id = #{id}
    </update>

    <update id="incCommentCount" parameterType="com.gk.university.model.Question">
    update question
    set
    COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=INTEGER}
    where id = #{id}
    </update>

    <select id="relatedQuestion" parameterType="com.gk.university.model.Question" resultMap="BaseResultMap">
        select * from question where TAG REGEXP #{tag} and id != #{id}
    </select>


    <select id="countQuestionBySearch" parameterType="com.gk.university.model.Question" resultType="Integer">
        select count(*) from question
        <where>
            <if test="search != null">
                and title REGEXP #{search}
            </if>

            <if test="tag != null">
                and tag REGEXP #{tag}
            </if>
        </where>

    </select>

    <select id="selectQuestionBySearch" parameterType="com.gk.university.dto.QuestionQueryDTO"
            resultMap="BaseResultMap">
        select * from question
        <where>
            <if test="search != null">
                and title REGEXP #{search}
            </if>

            <if test="tag != null">
                and tag REGEXP #{tag}
            </if>
        </where>
        order by gmt_create desc limit #{index},#{size}
    </select>

</mapper>